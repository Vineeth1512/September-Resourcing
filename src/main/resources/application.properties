

####### PostgreSQL Data-Source Properties #######
spring.datasource.url= jdbc:postgresql://localhost:5432/SEPTEMBER360
spring.datasource.username= postgres
spring.datasource.password= Pr@win08
#spring.datasource.driver-class-name=org.postgresql.Driver

#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true


###### JPA Properties ######
#Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.show-sql = true


spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.h2.console.enabled=true

spring.thymeleaf.cache=false

#Multipart file config
spring.servlet.multipart.enabled=true

# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=800MB
# Max Request Size
spring.servlet.multipart.max-request-size=800MB


spring.datasource.hikari.auto-commit=false
spring.http.multipart.enabled=false

#mail properties
spring.mail.host = smtp.gmail.com
spring.mail.port = 587
spring.mail.properties.mail.smtp.starttls.enable = true
spring.mail.username = t.srivani488@gmail.com
spring.mail.password = jodtwklidacpexti

spring.mail.properties.mail.smtp.starttls.required = true
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.connectiontimeout = 5000
spring.mail.properties.mail.smtp.timeout = 5000
spring.mail.properties.mail.smtp.writetimeout = 5000

spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.devtools.restart.pollInterval=1000s

server.servlet.session.timeout= 600s


